import { describe, expect, it } from "vitest";

// TODO: TESTS TESTS TESTS
describe.skip("Tabs component", () => {
    it("renders", () => {
        expect(true).toBeTruthy();
    });

    it("renders the provided tabs", () => {
        expect(true).toBeTruthy();
    });

    it("render correctly the selected element", () => {
        expect(true).toBeTruthy();
    });

    it("render correctly the disabled element", () => {
        expect(true).toBeTruthy();
    });

    it("emit a click when clicking a unselected tab", () => {
        expect(true).toBeTruthy();
    });

    it("doesn't emit a click when clicking a selected tab", () => {
        expect(true).toBeTruthy();
    });

    it("doesn't emit a click when clicking a disabled tab", () => {
        expect(true).toBeTruthy();
    });
});
